val x: Int = 1 + 2
// We can write it as val x = 3
// This is called referential transparency
println(x) // This just prints out the result, We cannot write it in any other form.

// if you call a method with same parameters and get the same result back always, that is called Referential transparency4, 16, 36, 64, 100)