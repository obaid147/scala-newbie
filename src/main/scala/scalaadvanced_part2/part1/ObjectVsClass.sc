/**
 * Objects are initialized when they are first referenced.
 * Unlike classes, there are no constructor parameters.
 * An object is a singleton. Singletons are the enemy of flexible configuration.
 * */